%%% =======================================================================
%%% fix for outdated matlab version
%%% =----------------------------------------------------------------------


function [out] = nanmean(x)
    ii  = isnan(x);
    out = mean(x(~ii));
end