%%% =======================================================================
%%% fix for outdated matlab version
%%% =----------------------------------------------------------------------


function [out] = nansum(x)
    ii  = isnan(x);
    out = sum(x(~ii));
end