%%% =======================================================================
%%% fix for outdated matlab version
%%% =----------------------------------------------------------------------


function [out] = nanmax(x)
    ii  = isnan(x);
    out = max(x(~ii));
end